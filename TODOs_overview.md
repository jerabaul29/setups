# TODOs overview

Centralized list of outstanding TODOs found in the codebase. When resolving one, remove it from this file.

- `main_setup.sh`: should sourcing really be used to inherit definitions (e.g. `.bashrc`) across steps, or is there a cleaner way?
- `main_setup.sh`: add a dummy/default git setup (e.g. `user.name`/`user.email`) as part of `setup_command_line`.
- `setup_standard_libraries/setup_standard_libraries.sh`: consider installing/setting up `podman`.
